#include<bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int N, K;
    ll P;
    cin >> N >> K >> P;

    vector<ll> A(N);
    for (int i = 0; i < N; i++) cin >> A[i];

    
}