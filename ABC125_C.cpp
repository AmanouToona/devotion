#include<bits/stdc++.h>
using namespace std;

template <template T>
struct SegmentTree {
    private:
        int n;
        vector<T> node;
    
    public:
        SegmentTree {

        }
}


int main() {
    int N;
    vector<int> A;

    cin >> N;
    for (int i = 0; i < N; i++) {
        int a;
        cin >> a;
        A.push_back(a);
    }
}
